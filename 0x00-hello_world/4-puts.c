#include <stdio.h>

/**
 * main - prints "Programming is like building a multimultilingual puzzle"
 *
 * Return: 0 if exited properly, non-zero if otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
